# Copy to: ~/.cloudflared/config.yml
# Replace placeholders: <TUNNEL-UUID>, <HOSTNAME>

tunnel: <TUNNEL-UUID>
credentials-file: /home/deb/.cloudflared/<TUNNEL-UUID>.json

ingress:
  - hostname: <HOSTNAME>
    service: http://localhost:4000
  - service: http_status:404
